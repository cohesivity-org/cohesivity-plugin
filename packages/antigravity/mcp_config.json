{
  "mcpServers": {
    "cohesivity": {
      "serverUrl": "https://cohesivity.ai/mcp"
    },
    "cohesivity-local": {
      "command": "node",
      "args": [
        "${extensionPath}${/}mcp${/}project-bootstrap.mjs"
      ],
      "cwd": "${extensionPath}"
    }
  }
}
